import { Route } from '@angular/router';
import { LoadingContainer } from './loading-container/loading-container';

export const loadingContainerRoutes: Route[] = [
  { path: '', component: LoadingContainer },
];
